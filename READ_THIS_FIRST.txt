HEART & SOUL — JSON READ FIX

We isolated the exact issue.

Netlify Blobs:
- `store.get(key, { type: "json" })` returns the parsed JSON object DIRECTLY.
- `getWithMetadata()` is the method that returns `{ data, etag, metadata }`.

The previous build incorrectly tried to read `.data`, which stripped the outer
order object and caused orderNumber/studentName to become null.

STEPS
1. Upload ALL contents of this ZIP to the ROOT of GitHub.
2. Commit and wait for Netlify to publish.
3. Confirm /orders/ says:
   BUILD: JSON-READ-FIX

4. Open:
https://heartandsoulmums.netlify.app/.netlify/functions/orders?storageTest=1

Expected:
"blobCount":1,
"firstRead":{
  "ok":true,
  "orderNumber":"HS-...",
  "studentName":"..."
}

5. Then open /orders/ and enter the owner password.

No environment-variable changes are required.
