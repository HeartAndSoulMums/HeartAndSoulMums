HEART & SOUL — BLOB READ FIX

We isolated the issue:
- Netlify Blobs LIST works.
- There is already 1 saved order.
- The previous code treated `store.get()` as if it returned the JSON value directly.

Current Netlify Blobs returns an entry object whose `.data` property contains the
requested JSON data. This build unwraps `.data` correctly.

STEPS
1. Upload ALL contents of this ZIP to the ROOT of GitHub.
2. Commit and wait for Netlify to publish.
3. Confirm /orders/ says:
   BUILD: BLOB-READ-FIX

4. Open:
https://heartandsoulmums.netlify.app/.netlify/functions/orders?storageTest=1

A successful response should now include something like:
"blobCount":1,
"firstRead":{"ok":true,"orderNumber":"HS-...","studentName":"..."}

5. Then open /orders/, enter the owner password, and the saved order should load.

No environment-variable changes are required.
