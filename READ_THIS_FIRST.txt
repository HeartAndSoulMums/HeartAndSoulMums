HEART & SOUL — LOGIN REQUEST FIX

The storage layer is proven working. This build changes ONLY the owner-dashboard request path.

The dashboard no longer sends the owner password in a custom x-owner-key request header.
Instead it sends a normal same-origin POST body:
  { action: "list", ownerKey: "..." }

This removes the remaining difference between the successful diagnostic request and the hanging dashboard request.

UPLOAD:
1. Upload ALL ZIP contents to the GitHub repo root.
2. Commit and wait for Netlify.
3. Confirm /orders/ says BUILD: LOGIN-FIX.
4. Enter the owner password and click Open Orders.

No environment variable changes.
The existing saved Jordan Davis order remains in Netlify Blobs and does NOT need to be resubmitted.
